# lib/format/

Pure presentation functions: INR from paise with lakh grouping, size conversion
across IN/UK/EU/US, relative dates.

No React, no I/O, no dependencies on `lib/data`. Every function here has a unit
test — these are the details that quietly go wrong in an Indian storefront.
