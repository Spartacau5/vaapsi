# lib/types/

The data contract. Written in Phase 1 and treated as the interface between this
storefront and the backend.

This is the file set the backend team maps Prisma models onto. Changing a type
here is a contract change: say so in the PR description.
