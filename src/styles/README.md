# styles/

Design tokens and global CSS. `tokens.css` is the only file in the repo allowed
to contain a hex code or a `font-family` declaration.

Colour presets, type scale, spacing, radius and motion all live here as CSS
custom properties so the runtime theme switcher is a config change, not a rewrite.
