# content/

All user-facing copy. English only for v1, but no string is hardcoded into JSX
so a locale layer can be added without touching components.

One module per surface (`home.ts`, `product.ts`, `passport.ts`, `cart.ts`) plus
`common.ts` for shared labels.
