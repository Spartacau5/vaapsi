# components/ui/

shadcn/ui primitives, generated by the CLI and left **unmodified**.

If a primitive needs to look different, change a token in `styles/tokens.css`.
If it needs to behave differently, wrap it in `components/primitives/`. Editing
files here makes future `shadcn add` runs a merge conflict.
