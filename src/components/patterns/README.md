# components/patterns/

Composed, product-aware components — `ProductCard`, `PassportSeal`,
`ConditionScale`, `PriceBlock`.

These import from `ui/` and `primitives/`, read types from `lib/types` and copy
from `content/`. They never import fixtures.
