# app/

Routes only. Segments, layouts, loading and error boundaries.

Keep these files thin: fetch through `lib/data`, render components from
`components/`, take copy from `content/`. No business logic, no fixtures, no
formatting rules here.
